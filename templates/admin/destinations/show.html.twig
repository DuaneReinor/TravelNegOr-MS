{% extends 'admin/base.html.twig' %}

{% block title %}Destination Details{% endblock %}

{% block admin_content %}
{% from 'admin/shared/show_page_layout.html.twig' import render_show_page %}

{% set fields_config = [
    {'name': 'id', 'label': 'ID', 'type': 'text'},
    {'name': 'name', 'label': 'Destination Name', 'type': 'text'},
    {'name': 'location', 'label': 'Location', 'type': 'text'},
    {'name': 'description', 'label': 'Description', 'type': 'text'},
    {'name': 'image', 'label': 'Destination Image', 'type': 'image', 'show_empty': true},
    {'name': 'createdAt', 'label': 'Created At', 'type': 'date'}
] %}

{{ render_show_page(
    destination,
    path('admin_destinations_index'),
    'Destinations',
    destination.name,
    fields_config,
    path('admin_destinations_edit', {'id': destination.id}),
    'admin/destinations/_delete_form.html.twig'
) }}
{% endblock %}
